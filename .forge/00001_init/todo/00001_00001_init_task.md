# Task 00001: Project scaffolding — pyproject.toml, directory layout, and config.yaml

## Role
programmer

## Objective
Create the complete project scaffold for the `corpus_council` Python package: `pyproject.toml` (declaring the package, all dependencies, tool configuration), the full directory tree from `project.md`, a working `config.yaml` with all configurable values (no hardcoded secrets), and empty `__init__.py` files that make the package importable. After this task, `uv run python -c "import corpus_council"` must succeed and `uv run ruff check src/` must exit 0.

## Context
The project root is `/home/buddy/projects/corpus-council/`. No source files exist yet — this task creates the entire skeleton. The tech stack is Python 3.12+, `uv` as build tool and package manager, FastAPI, Typer, ChromaDB, sentence-transformers, anthropic SDK, PyYAML, pytest, ruff, mypy.

The directory layout from `project.md`:
```
corpus-council/
  corpus/          # flat file knowledge (.md, .txt) — create empty dir with .gitkeep
  council/         # persona markdown files — create empty dir with .gitkeep
  templates/       # LLM prompt templates (.md) — create empty dir with .gitkeep
  plans/           # collection plan markdown files — create empty dir with .gitkeep
  data/            # runtime-generated, already in .gitignore
  config.yaml
  src/
    corpus_council/
      __init__.py
      api/
        __init__.py
        routers/
          __init__.py
      cli/
        __init__.py
      core/
        __init__.py
  tests/
    __init__.py
    unit/
      __init__.py
    integration/
      __init__.py
  pyproject.toml
```

`config.yaml` must configure (no hardcoded values, all overridable):
- `llm.provider` (default: `anthropic`)
- `llm.model` (default: `claude-3-5-haiku-20241022`)
- `embedding.provider` (default: `sentence-transformers`)
- `embedding.model` (default: `all-MiniLM-L6-v2`)
- `data_dir` (default: `data`)
- `corpus_dir` (default: `corpus`)
- `council_dir` (default: `council`)
- `templates_dir` (default: `templates`)
- `plans_dir` (default: `plans`)
- `chunking.max_size` (default: `512`, characters per chunk)
- `retrieval.top_k` (default: `5`)

No API keys in `config.yaml` — they come from environment variables only.

## Steps
1. Create `pyproject.toml` at `/home/buddy/projects/corpus-council/pyproject.toml` with:
   - `[build-system]` using `hatchling`
   - `[project]` with name `corpus-council`, version `0.1.0`, Python `>=3.12`, all dependencies
   - `[project.scripts]` with `corpus-council = "corpus_council.cli.main:app"`
   - `[tool.pytest.ini_options]` with `addopts = "--cov=src/corpus_council/core --cov-fail-under=80"`, `testpaths = ["tests"]`
   - `[tool.mypy]` with `strict = true`, `python_version = "3.12"`
   - `[tool.ruff.lint]` with sensible defaults
   - `[tool.coverage.run]` with `source = ["src/corpus_council/core"]`
   Dependencies to include: `fastapi>=0.115`, `uvicorn[standard]>=0.32`, `typer>=0.13`, `chromadb>=0.5`, `sentence-transformers>=3.0`, `anthropic>=0.40`, `PyYAML>=6.0`, `pydantic>=2.9`, `python-frontmatter>=1.1`
   Dev dependencies: `pytest>=8.3`, `pytest-cov>=6.0`, `pytest-asyncio>=0.24`, `httpx>=0.27`, `mypy>=1.13`, `ruff>=0.8`, `types-PyYAML>=6.0`
2. Create all `__init__.py` files listed in the directory layout above. Each is empty except `src/corpus_council/__init__.py` which contains `__version__ = "0.1.0"`.
3. Create `corpus/`, `council/`, `templates/`, `plans/` directories each with a `.gitkeep` file.
4. Create `config.yaml` at project root with all configuration keys listed in Context, using the specified defaults. Include a comment at the top: `# API keys are not stored here — use environment variables (ANTHROPIC_API_KEY, etc.)`.
5. Run `uv sync` to install dependencies.
6. Verify import works.

## Verification
- File `/home/buddy/projects/corpus-council/pyproject.toml` exists and contains `[project.scripts]` with `corpus-council` entry
- File `/home/buddy/projects/corpus-council/config.yaml` exists
- File `/home/buddy/projects/corpus-council/src/corpus_council/__init__.py` exists
- File `/home/buddy/projects/corpus-council/src/corpus_council/core/__init__.py` exists
- File `/home/buddy/projects/corpus-council/src/corpus_council/api/__init__.py` exists
- File `/home/buddy/projects/corpus-council/src/corpus_council/cli/__init__.py` exists
- `grep -r "api_key\|ANTHROPIC_API_KEY\s*=" /home/buddy/projects/corpus-council/config.yaml` returns no matches (no secrets in config)
- `uv run python -c "import corpus_council"` exits 0
- `uv run ruff check src/` exits 0
- `uv run ruff format --check src/` exits 0
- Dynamic: `uv run python -c "import corpus_council; print(corpus_council.__version__)"` prints `0.1.0`

## Done When
- [ ] `pyproject.toml` exists with all dependencies and tool configuration
- [ ] All package `__init__.py` files exist
- [ ] `config.yaml` exists with all keys and no secrets
- [ ] `uv run python -c "import corpus_council"` exits 0
- [ ] All verification checks pass

## Save Command
```
git add -A && git commit -m "task-00001: project scaffolding — pyproject.toml, directory layout, and config.yaml"
```
